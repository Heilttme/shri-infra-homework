name: Docker deploy

on:
  workflow_run:
    workflows: ["Create Release"]
    types:
      - requested

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Login Yandex Cloud
        id: login-cr
        uses: yc-actions/yc-cr-login@v1
        with:
          yc-sa-json-credentials: ${{ secrets.AUTHORIZED_KEY }} 

      - name: Build docker image
        env:
          CR_REGISTRY: crpkb2sscjdrc12okn1d
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          docker build -t cr.yandex/$CR_REGISTRY/app:${{ github.run_number }} .
          docker build -t cr.yandex/$CR_REGISTRY/app:${{ github.run_number }}_latest .
          docker push cr.yandex/$CR_REGISTRY/app:${{ github.run_number }}
          docker push cr.yandex/$CR_REGISTRY/app:${{ github.run_number }}_latest
